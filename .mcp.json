{
  "mcpServers": {
    "MCP_DOCKER": {
      "command": "docker",
      "args": ["mcp", "gateway", "run"],
      "env": {
        "HOME": "/Users/ariegoldkin",
        "PATH": "/usr/local/bin:/usr/bin:/bin"
      }
    }
  }
}
